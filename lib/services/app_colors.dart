import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF007AFF);      // blu principale (scegli tu)
  static const neutral = Color(0xFF666666);      // testo / icone secondarie
  static const background = Colors.white;        // sfondo
  static const error = Colors.red;               // errori / snackbar
  
}